services:
  db:
    image: postgres:alpine
    container_name: db
    restart: always
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    ports:
      - '5434:5432'
    volumes:
      - db-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres']
      interval: 1s
      timeout: 1s
      retries: 15

  server:
    build:
      context: ./node/express-prisma-typescript
      dockerfile: Dockerfile
      target: dev
    container_name: server
    restart: always
    tty: true
    environment:
      - DATABASE_URL=${DOCKER_DATABASE_URL}
      - PORT=8080
    env_file:
      - .envrc
    ports:
      - '8080:8080'
    volumes:
      - ./node/express-prisma-typescript/src:/app/src
      - ./node/express-prisma-typescript/prisma:/app/prisma
    depends_on:
      db:
        condition: service_healthy
    links:
      - db

volumes:
  db-data:
